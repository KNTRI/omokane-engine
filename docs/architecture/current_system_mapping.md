# 現行システム対応表

## 1. 目的

世界知能カーネルの新概念を、現在のv0.1コードと設計へ対応付ける。
`Existing`、`Partial`、`Missing`、`Deferred`、`Conflict` を使い、存在しない実装をあるものとして扱わない。

## 2. 対応表

| 新概念 | 神話名 | 日本語API | 現在の型・モジュール | 状態 | 差分 | 移行方針 | ADR |
|---|---|---|---|---|---|---|---|
| 世界真実 | 思兼神Core | 世界知能核 / 世界真実 | `ゲーム状態`、`思兼神.更新する` | Partial | v0.1状態は存在するがTruth境界は未定義 | 権威状態を明示し、観測・表現から分離する | ADR-001, ADR-002 |
| 因果操作 | 因果Transaction | 因果操作 | `因果操作` 契約型、`因果操作実行.適用する`、`因果操作列実行.一括適用する` | Partial | Entity位置変更操作だけを入力順で原子的に一括適用できるが、異種操作・再生・Lock・裁定は未実装 | 既存Movementと並行して、入力・権能・環境反応を段階的に操作へ変換する | ADR-003 |
| 因果台帳 | 因果Ledger | 因果台帳 | `因果台帳記録`、`因果台帳`、`因果台帳.追記する` | Partial | 成功したEntity位置変更記録を入力順で原子的に追記できるが、永続化・異種記録・再生は未実装 | 正式履歴とDebug情報を分離したまま保存・再生境界を追加する | ADR-008 |
| 状態場 | 八百万Field | 状態場 | 気配Fieldの設計記述のみ | Deferred | 共通Field契約、空間解像度、保存形式なし | 箱庭垂直スライスで1種のFieldから始める | ADR-005, ADR-009 |
| 物理情報伝播 | 風土Propagation | 物理情報伝播 | `伝播信号` 契約型 | Partial | 伝播演算、媒体特性、センサー変換なし | 1次元の決定論モデルを別タスクで追加する | ADR-006, ADR-009 |
| 散逸状態 | 黄泉Entropy | 散逸状態 | `散逸状態` 契約型 | Partial | 時間発展、相互作用、直毘Operationなし | 種別ごとの規則を個別に追加する | ADR-009 |
| 感知値 | 八咫烏Observe | 感知値 | `感知値` 契約型 | Partial | センサー能力、遮蔽、誤差、遅延の計算なし | 伝播信号からの最小変換を追加する | ADR-006, ADR-009 |
| 観測 | 八咫烏Observe | 観測 | `観測` 契約型、既存の観測更新設計 | Partial | 生成処理、更新履歴、期限なし | 感知値だけを根拠にする変換を追加する | ADR-002, ADR-006 |
| 信念 | 記紀Memory | 信念 / 信念候補 | `信念候補` 契約型 | Partial | 採択、反証、忘却、推論なし | 観測から候補を作る最小純粋関数を追加する | ADR-006, ADR-008 |
| 地表記憶 | 国津Memory | 地表記憶 | 痕跡Asset・場所記憶Assetの設計記述のみ | Deferred | 権威痕跡型と保存規則なし | 表現デカールと分離した痕跡契約を設計する | ADR-002, ADR-004 |
| 身体状態 | 御身State | 身体状態 | `エンティティ` の位置、速度、HP | Partial | 身体材質域、装具拘束、接触解釈なし | 御魂Stateと分離した別契約として追加する | ADR-007 |
| 権能操作 | 神威Operation | 権能操作 | `権能Asset`、`因果操作種別.権能操作` | Partial | Asset定義はあるが操作・競合・残滓なし | 権能Assetから因果操作を生成する境界を追加する | ADR-003, ADR-009 |
| 常駐制御 | 鎮座Scheduler | 常駐制御 | なし | Deferred | 詳細/遠景Sim、優先度、休眠状態なし | 意味保存LODの要件確定後に設計する | ADR-005 |
| 再生・可観測性 | 記紀Replay / 神託Debug | 再生履歴 / 判断可視化 | `ゲームイベント`、`因果台帳`、因果Log・神託Debug設計文書 | Partial | インメモリ台帳はあるが、永続化・スナップショット・再生器・Debug出力なし | 台帳の決定論的な保存・再生契約を段階的に追加する | ADR-008 |

ADRリンクは [アーキテクチャ索引](index.md) から参照する。

## 3. 現行直接更新の扱い

現在の `思兼神.更新する` は、入力一覧を現在状態へ直接適用し、プレイヤーのX座標とTickを更新している。
世界知能カーネルの最終経路とは異なるが、v0.1の通電確認として許容する。

今回リファクタリングせず、Movement Smokeも変更しない。
因果操作最小実行器はEntity位置変更だけを扱う並行経路として追加し、操作適用ではTickを進めない。
因果操作列の一括適用は、全件静的検証後に入力順で仮適用し、全件成功時だけEventと記録候補を公開する。
因果台帳の最小追記は、成功した記録候補だけを入力順で原子的に正式履歴へ変換し、失敗記録と因果操作ID重複を拒否する。
将来的には次へ移行する。

```text
入力意図
→ 因果操作
→ 天鳥船Motion
→ 世界状態反映
→ 言霊Event / 因果台帳
```

この差分は [ADR-003](../adr/ADR-003-causal-transaction.md) に記録する。

## 4. 権能Asset公開契約の扱い

現在の `権能Asset.発行Event一覧` はv0.1互換として維持し、今回変更しない。
将来的には次へ移行する。

```text
権能Asset
→ 神威Operation
→ 因果操作
→ Event / Trace / 因果台帳
```

移行時も既存Assetを黙って読み替えず、版管理または変換境界を設ける。

## 5. 今回の非目標

契約型とインメモリ台帳の追加は、実行基盤の完成を意味しない。
異種因果操作、因果台帳の永続化と再生、伝播計算、AI判断、地表記憶、身体システム、LOD、常駐制御は今後の独立作業とする。
